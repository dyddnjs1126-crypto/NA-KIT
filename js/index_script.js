$(document).ready(function() {
    
    // [제이쿼리 센서 가동] 이미지 로딩 감지
    $(window).on('load', function() {
        
        /* 
           화면 전체를 지우던 .fadeOut()과 .remove() 구문을 완전히 제거했습니다.
           이제 5개의 로고 조각이 순서대로 웅장하게 조립된 직후, 
           그 자리에 가만히 멈춘 상태에서 [홈으로 가기] 버튼이 슥 노출되어 고정됩니다.
        */
        
    });

});